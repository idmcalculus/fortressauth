{
  "name": "fortressauth",
  "version": "0.1.9",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage --filter=./packages/** --filter=!./examples/** --filter=!./landing/**",
    "test:coverage:all": "turbo run test:coverage",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "concurrently": "^9.2.1",
    "turbo": "^2.7.1",
    "typescript": "^5.9.3"
  }
}
