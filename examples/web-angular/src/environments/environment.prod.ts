/**
 * FortressAuth Angular Example - Environment Configuration
 * Production environment settings.
 */

export const environment = {
  production: true,
  // In production, this should be set via environment variable or build configuration
  apiUrl: 'https://api.example.com',
};
