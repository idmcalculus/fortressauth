[build]
  command = "pnpm exec turbo run build --filter=fortressauth-web-vue"
  publish = "examples/web-vue/dist"
  ignore = "node scripts/netlify-build-policy.mjs vue"

[build.environment]
  DEMO_BASE_PATH = "/"
  VITE_API_URL = "https://api.fortressauth.com/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
